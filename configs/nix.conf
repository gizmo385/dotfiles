experimental-features = nix-command flakes

# Attic cache configuration
# Add your cache as a substituter (will try cache.nixos.org first, then your cache)
extra-substituters = https://attic.alpenglow.acbc.house/
# Add the cache's public key (get with: attic cache info ci-cache)
extra-trusted-public-keys = ci-cache:BXz8Qym/dtncWHsc5VWZY+Se/GCM3gfc1k/5IwRPV5o=
